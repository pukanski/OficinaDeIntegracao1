CREATE TABLE IF NOT EXISTS "__EFMigrationsHistory" (
    "MigrationId" character varying(150) NOT NULL,
    "ProductVersion" character varying(32) NOT NULL,
    CONSTRAINT "PK___EFMigrationsHistory" PRIMARY KEY ("MigrationId")
);

START TRANSACTION;
CREATE TABLE "Turmas" (
    "ID" bigint GENERATED BY DEFAULT AS IDENTITY,
    "Nome" character varying(80) NOT NULL,
    "Ano" character varying(4) NOT NULL,
    "Turno" character varying(20) NOT NULL,
    "CriadoEm" timestamp with time zone NOT NULL,
    "AtualizadoEm" timestamp with time zone,
    CONSTRAINT "PK_Turmas" PRIMARY KEY ("ID")
);

CREATE TABLE "Aluno_Turma" (
    "AlunoId" bigint NOT NULL,
    "TurmaId" bigint NOT NULL,
    "MatriculadoEm" timestamp with time zone NOT NULL,
    CONSTRAINT "PK_Aluno_Turma" PRIMARY KEY ("AlunoId", "TurmaId"),
    CONSTRAINT "FK_Aluno_Turma_Turmas_TurmaId" FOREIGN KEY ("TurmaId") REFERENCES "Turmas" ("ID") ON DELETE CASCADE
);

CREATE TABLE "Professor_Turma" (
    "ProfessorId" bigint NOT NULL,
    "TurmaId" bigint NOT NULL,
    "VinculadoEm" timestamp with time zone NOT NULL,
    CONSTRAINT "PK_Professor_Turma" PRIMARY KEY ("ProfessorId", "TurmaId"),
    CONSTRAINT "FK_Professor_Turma_Turmas_TurmaId" FOREIGN KEY ("TurmaId") REFERENCES "Turmas" ("ID") ON DELETE CASCADE
);

CREATE INDEX "IX_Aluno_Turma_TurmaId" ON "Aluno_Turma" ("TurmaId");

CREATE INDEX "IX_Professor_Turma_TurmaId" ON "Professor_Turma" ("TurmaId");

CREATE UNIQUE INDEX "IX_Turmas_Nome_Ano_Turno" ON "Turmas" ("Nome", "Ano", "Turno");

INSERT INTO "__EFMigrationsHistory" ("MigrationId", "ProductVersion")
VALUES ('20260622144303_InitialTurma', '9.0.0');

COMMIT;

